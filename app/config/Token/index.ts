const JWT_PASSWORD : string = "password_golden_app_jwt";
const KEY_TOKEN : string = "token";
const MESSAGES  = {
    success : "valid token",
    failed : "invalid token",
    expired : "expired token"
}

export {
    JWT_PASSWORD,
    MESSAGES,
    KEY_TOKEN
}