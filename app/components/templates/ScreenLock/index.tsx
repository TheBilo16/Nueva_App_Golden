import React, { FC } from "react";
import { View } from "react-native";
import styles from "./styles";

const ScreenLock : FC = () : JSX.Element => {
    return <View>

    </View>
}

export default ScreenLock;