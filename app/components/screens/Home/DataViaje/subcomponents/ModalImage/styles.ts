import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    modal_content : {
        width : "100%",
        height : 210,
        backgroundColor : "#fff"
    }
});

export default styles;